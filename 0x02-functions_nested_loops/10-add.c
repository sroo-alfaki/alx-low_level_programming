#include "main.h"

/**
 * add -  function that adds two integers and returns the result.
 *
 * @x :first input
 * @y : secand input
 *
 * Return: sum of x & y
 */
int add(int x, int y)
{
	int s = x + y;

	return (s);
}
